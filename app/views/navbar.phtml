<nav class="navbar navbar-inverse navbar-fixed-top always-open">
    <div class="container">
        <div class="navbar-header">
            <?php if(isset($menu)) : ?>
            <button type="button" id="menu_btn" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <?php endif ?>
            <a class="navbar-brand" href="/portal/">
                <img src="<?php echo $this->config->application->baseUri ?>img/icons/favicons/favicon-32x32.png" />
                <span><?php echo $title ?></span>
            </a>
        </div>

        <div id="navbar" class="collapse navbar-collapse">
            <?php if(isset($menu)) : ?>
            <ul class="nav navbar-nav">
                <?php foreach($menu->getItems(array('order' => 'name ASC')) as $menuItem) : ?>
                <?php if(strpos($menuItem->url, $this->config->network->broadcast) !== false && strpos($_SERVER['REMOTE_ADDR'], $this->config->network->broadcast) === false) continue ?>

                <li class="col-xs-6 col-sm-3 col-lg-2 hover<?php echo $menuItem->device != null ? ' disabled" data-ip="' . $menuItem->device->ip . '"' : '"' ?>>
                    <a href="<?php echo $menuItem->url ?>" target="_blank">
                        <img src="<?php echo $this->config->application->baseUri ?>img/icons/menu/<?php echo (!empty($menuItem->icon) ? $menuItem->icon : 'unknown.ico') ?>" width="16" />
                        <?php echo $menuItem->name ?>
                    </a>
                </li>
                <?php endforeach ?>
            </ul>
            <div class="col-xs-6 user no-gutter">
                <a class="btn navbar-toggle pull-left" href="<?php echo $this->config->application->baseUri ?>session/logout/">
                    <span class="glyphicon glyphicon-user" title="<?php echo $this->session->get('auth')['username'] ?>"></span>
                    &nbsp;&nbsp;&nbsp;Logout
                </a>
            </div>
            <div class="col-xs-6 no-gutter">
                <a class="btn navbar-toggle pull-right settings" href="<?php echo $this->config->application->baseUri ?>settings/">
                    <span class="glyphicon glyphicon-cog"></span>
                </a>
            </div>
            <?php endif ?>
        </div>
    </div>
</nav>