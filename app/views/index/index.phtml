<div class="row">
    <div class="col-xs-12 col-md-8">
        <div class="panel panel-success pull-left harddisks">
            <div class="panel-heading">
                <a href="#" class="btn btn-default glyphicon glyphicon-refresh pull-right disabled"></a>
                <h4>Disk usage</h4>
            </div>
            <ul class="list-group">
                <?php foreach($disks as $disk => $content) : ?>
                <li class="list-group-item col-xs-12 col-md-12">
                    <div class="col-xs-3">
                        <?php echo $disk ?>
                    </div>
                    <div class="col-xs-9">
                        <div class="progress">
                            <div class="progress-bar progress-bar-<?php echo $content['class'] ?> progress-bar-striped" role="progressbar" style="width:<?php echo $content['used_percentage'] ?>%">
                            </div>
                            <div><?php echo $content['used_percentage'] ?>% used</div>

                        </div>
                    </div>
                </li>
                <?php endforeach ?>
            </ul>
        </div>
    </div>

    <div class="col-xs-12 col-md-4">
        <div class="panel panel-success pull-left devices">
            <div class="panel-heading">
                <a href="#" class="btn btn-default glyphicon glyphicon-refresh pull-right"></a>
                <h4>Devices</h4>
            </div>
            <ul class="list-group">
                <?php foreach($devices as $device) : ?>
                <li class="list-group-item col-xs-12 col-md-12">
                    <div class="col-xs-7 col-md-6">
                        <?php echo $device->name ?>
                    </div>
                    <div class="col-xs-5 col-md-6">
                        <a class="btn btn-default disabled devicestate" data-ip="<?php echo $device->ip ?>" data-mac="<?php echo $device->mac ?>">
                            <span class="glyphicon glyphicon-refresh icon-refresh-animate"></span>
                        </a>
                        <?php if(!empty($device->webtemp)) : ?>
                        <a href="devices/webtemp/?id=<?php echo $device->id ?>" class="fancybox iframe btn btn-default" title="Webtemp: <?php echo $device->name ?>">
                            <span class="glyphicon glyphicon-dashboard"></span>
                        </a>
                        <?php endif ?>
                    </div>
                </li>
                <?php endforeach ?>
            </ul>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-xs-12 col-md-12">
        <div class="panel panel-success cpu">
            <div class="panel-heading">
                <h4>CPU</h4>
            </div>
            <div class="panel-body">
                <iframe src="/rcpu/"></iframe>
            </div>
        </div>
    </div>
</div>
