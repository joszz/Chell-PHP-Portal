<?php /** @var Phalcon\Mvc\Controller $this */ ?>

<div class="vertical-center login">
    <div class="panel panel-success">
        <div class="panel-heading">
            <h4>Login</h4>
            <button class="btn btn-default fa fa-fingerprint pull-right" id="webauth"></button>
        </div>
        <div class="panel-body">
            <p class="text-danger text-bold text-center">
                <?php echo $loginMessage ?>
            </p>
            <form action="<?php echo $form->getAction() ?>" method="post">
                <?php foreach($form AS $element) : ?>

                <?php if (is_a($element, Phalcon\Forms\Element\Password::class)) : ?>
                <div class="input-group" id="password-group">
                    <?php echo $element ?>
                    <a class="btn btn-default input-group-addon toggle-password vibrate waves-effect" title="Toggle password display">
                        <i class="fa fa-eye"></i>
                    </a>
                </div>
                <?php elseif (is_a($element, Phalcon\Forms\Element\Check::class)) : ?>
                <div id="rememberme-wrapper">
                    <?php echo $element ?>
                    <?php echo $element->label() ?>
                </div>
                <?php else : ?>
                <?php echo $element ?>
                <?php endif ?>
                <?php endforeach ?>

                <input type="hidden" name="webauth" value="" />
                <input type="hidden" name="<?php echo $this->security->getTokenKey() ?>" value="<?php echo $this->security->getToken() ?>" />
                <button type="submit" class="btn btn-lg btn-success btn-block">Login</button>
            </form>
        </div>
    </div>
</div>